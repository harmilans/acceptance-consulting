# Copy this file to .env for local testing with `vercel dev`.
# Do NOT put your real key in this file if you commit it to git —
# use `.env.local` (already gitignored) or Vercel's dashboard env vars instead.

ANTHROPIC_API_KEY=sk-ant-your-real-key-here
